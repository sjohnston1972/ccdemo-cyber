# Environment variables and credentials
.env

# Project-local SSH host-key trust store (populated by --accept-new-hostkey
# TOFU onboarding). Contains real device host keys - never commit it.
known_hosts

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Audit output files - never committed by default; they can contain real
# internal IPs, hostnames, and topology details for the audited environment.
audit_report_*.txt
